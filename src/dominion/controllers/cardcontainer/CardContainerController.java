package dominion.controllers.cardcontainer;

import dominion.controllers.Controller;

/*
    CardContainers don't have their own UIs,
    but cards inside them have.
    Therefore CardContainerController controls the UI of cards in the container,
    arrange cards to the corresponding position.
 */
public abstract class CardContainerController extends Controller {
}
